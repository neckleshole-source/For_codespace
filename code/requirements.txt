# Pinned for reproducibility. Bump deliberately only after re-validating.
# CPU and CUDA both work; the same requirements.txt covers both.
# Install with: pip install -r code/requirements.txt

torch>=2.1,<3
torchvision>=0.16,<1
timm>=1.0,<2
albumentations>=2.0,<3
opencv-python-headless>=4.8
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
pyyaml>=6.0
Pillow>=10.0
tqdm>=4.65
kaggle>=1.6
